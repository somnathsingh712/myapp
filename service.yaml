apiVersion: v1
kind: Service
metadata:
  name: myapp   # keep consistent
  labels:
    app: myapp
spec:
  type: NodePort
  selector:
    app: myapp   # 🔥 THIS MUST MATCH DEPLOYMENT
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007